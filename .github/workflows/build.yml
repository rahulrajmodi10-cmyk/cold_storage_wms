name: Build & Release

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    name: Dart Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for sqflite_common_ffi
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: flutter-deps-${{ hashFiles('pubspec.yaml', 'pubspec.lock') }}
          restore-keys: |
            flutter-deps-
      - name: Verify pubspec.lock exists
        run: ls -la pubspec.lock
      - name: Get dependencies
        run: |
          for i in 1 2 3; do
            echo "Attempt $i of 3..."
            flutter pub get --verbose && break
            echo "Attempt $i failed, waiting 15 seconds..."
            sleep 15
          done
      - name: Analyze
        run: |
          echo "=== Dart SDK version ==="
          dart --version
          echo "=== Running dart analyze on lib/models/ lib/services/ ==="
          dart analyze lib/models/ lib/services/ --fatal-infos 2>&1 | tee analyze_output.txt || true
      - name: Upload analyze output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: analyze-output
          path: analyze_output.txt

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for sqflite_common_ffi
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: flutter-deps-${{ hashFiles('pubspec.yaml', 'pubspec.lock') }}
          restore-keys: |
            flutter-deps-
      - name: Verify pubspec.lock exists
        run: ls -la pubspec.lock
      - name: Get dependencies
        run: |
          for i in 1 2 3; do
            echo "Attempt $i of 3..."
            flutter pub get --verbose && break
            echo "Attempt $i failed, waiting 15 seconds..."
            sleep 15
          done
      - name: Run tests
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter test test/ ==="
          flutter test test/ --verbose 2>&1 | tee test_output.txt || true
      - name: Upload test output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: test_output.txt

  build-android:
    name: Build Android APK & App Bundle
    runs-on: ubuntu-latest
    needs: [analyze, test]
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for sqflite_common_ffi
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: flutter-deps-${{ hashFiles('pubspec.yaml', 'pubspec.lock') }}
          restore-keys: |
            flutter-deps-
      - name: Verify pubspec.lock exists
        run: ls -la pubspec.lock
      - name: Get dependencies
        run: |
          for i in 1 2 3; do
            echo "Attempt $i of 3..."
            flutter pub get --verbose && break
            echo "Attempt $i failed, waiting 15 seconds..."
            sleep 15
          done
      - name: Build APK (release)
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter build apk --release --split-per-abi ==="
          flutter build apk --release --split-per-abi --verbose 2>&1 | tee build_apk_output.txt || true
      - name: Build App Bundle (release)
        run: |
          echo "=== Running flutter build appbundle --release ==="
          flutter build appbundle --release --verbose 2>&1 | tee build_aab_output.txt || true
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-android-logs
          path: |
            build_apk_output.txt
            build_aab_output.txt
      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-apks
          path: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/bundle/release/*.aab
          retention-days: 30

  build-web:
    name: Build Web
    runs-on: ubuntu-latest
    needs: [analyze]
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for sqflite_common_ffi
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: flutter-deps-${{ hashFiles('pubspec.yaml', 'pubspec.lock') }}
          restore-keys: |
            flutter-deps-
      - name: Verify pubspec.lock exists
        run: ls -la pubspec.lock
      - name: Get dependencies
        run: |
          for i in 1 2 3; do
            echo "Attempt $i of 3..."
            flutter pub get --verbose && break
            echo "Attempt $i failed, waiting 15 seconds..."
            sleep 15
          done
      - name: Build Web (release)
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter build web --release ==="
          flutter build web --release --verbose 2>&1 | tee build_web_output.txt || true
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-web-logs
          path: build_web_output.txt
      - name: Upload Web artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-web
          path: build/web/
          retention-days: 30

  build-desktop:
    name: Build Desktop (Linux/macOS/Windows)
    runs-on: ubuntu-latest
    needs: [analyze]
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for sqflite_common_ffi (Linux only)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: flutter-deps-${{ hashFiles('pubspec.yaml', 'pubspec.lock') }}
          restore-keys: |
            flutter-deps-
      - name: Get dependencies
        run: |
          for i in 1 2 3; do
            echo "Attempt $i of 3..."
            flutter pub get --verbose && break
            echo "Attempt $i failed, waiting 15 seconds..."
            sleep 15
          done
      - name: Build Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter build linux --release ==="
          flutter build linux --release --verbose 2>&1 | tee build_linux_output.txt || true
      - name: Build macOS
        if: matrix.os == 'macos-latest'
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter build macos --release ==="
          flutter build macos --release --verbose 2>&1 | tee build_macos_output.txt || true
      - name: Build Windows
        if: matrix.os == 'windows-latest'
        run: |
          echo "=== Flutter version ==="
          flutter --version
          echo "=== Running flutter build windows --release ==="
          flutter build windows --release --verbose 2>&1 | tee build_windows_output.txt || true
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-desktop-logs-${{ runner.os }}
          path: |
            build_linux_output.txt
            build_macos_output.txt
            build_windows_output.txt
      - name: Upload Desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ runner.os }}
          path: |
            build/linux/**/*
            build/macos/**/*
            build/windows/**/*
          retention-days: 30

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-android, build-web, build-desktop]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Setup GitHub CLI
        run: |
          sudo apt-get update && sudo apt-get install -y gh
      - name: List available artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "=== Available artifacts in this workflow run ==="
          gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts --jq '.artifacts[] | "\(.name) (\(.size_in_bytes) bytes)"'
      - uses: actions/download-artifact@v4
        with:
          name: release-apks
          path: artifacts/apks
      - uses: actions/download-artifact@v4
        with:
          name: release-web
          path: artifacts/web
      - uses: actions/download-artifact@v4
        with:
          name: release-Linux
          path: artifacts/linux
      - uses: actions/download-artifact@v4
        with:
          name: release-macOS
          path: artifacts/macos
      - uses: actions/download-artifact@v4
        with:
          name: release-Windows
          path: artifacts/windows
      - name: List downloaded files
        run: |
          echo "=== Downloaded artifacts ==="
          find artifacts -type f | head -50
          echo "=== Directory structure ==="
          ls -la artifacts/
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Cold Storage WMS ${{ github.ref_name }}
          body: |
            ## Cold Storage Warehouse Management System

            ### Changes
            - Automated build from tag ${{ github.ref_name }}

            ### Downloads
            - **Android APKs** (arm64, arm32, x64): `app-release-*.apk`
            - **Android App Bundle**: `app-release.aab`
            - **Web**: Extract and host on any static hosting
            - **Desktop**: Platform-specific builds in respective folders

            ### Installation
            ```bash
            # Android
            adb install app-release-arm64-v8a.apk

            # Web
            # Host contents of web/ folder on any static hosting

            # Desktop
            # Run the executable in the respective platform folder
            ```
          files: |
            artifacts/apks/*.apk
            artifacts/apks/*.aab
            artifacts/web/**
            artifacts/linux/**
            artifacts/macos/**
            artifacts/windows/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}